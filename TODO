IDEAS:
* defining colors with hsl() function + eval function hsl()
* improve return structure of eval parser
* include osm data for tests
* unit tests
* improve profiling
* join: join osm objects with rows from an external csv file to import additional tags
* diagrams: draw diagrams to images in tmp directory on disk, which can be references by Mapnik, e.g. bar diagrams

NEEDS WORK:
* scaling icons
* Eval: zmetric
* extrude- // very ugly when using BuildingSymbolizer

NOT POSSIBLE RIGHT NOW:
* text-decoration
* font-variant
